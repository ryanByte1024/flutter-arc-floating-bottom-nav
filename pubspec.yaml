name: arc_floating_bottom_nav
description: A customizable floating bottom navigation bar for Flutter with a raised center item.
version: 0.1.1
topics:
  - flutter
  - navigation
  - bottom-navigation
  - ui

environment:
  sdk: ^3.11.4

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
